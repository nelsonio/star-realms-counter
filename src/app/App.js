import React from 'react';

import {AppLayout} from '../components';
import Player from './Player';

const App = () => (
    <AppLayout>
        <Player />
        <Player />
    </AppLayout>
);

export default App;
